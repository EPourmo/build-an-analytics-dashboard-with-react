/**
 * Class representing a user sessions
 */
export default class Sessions {
  /**
   * average sessions of user
   * @param {Array} sessions
   */
  constructor(sessions) {
    this.sessions = sessions;
  }
}
